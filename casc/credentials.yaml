credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "slack-token"
              description: "Slack bot token"
              secret: "${SLACK_BOT_TOKEN}"
          - string:
              scope: GLOBAL
              id: "docker-password"
              description: "Docker registry password"
              secret: "${readFile:/run/secrets/docker-password}"